# Worktrees
.worktrees/

# Ephemeral plan files (session-specific)
.plans/

# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
lazyboards

# Test binary
*.test

# Output
*.out

# GoReleaser
dist/

# Go workspace
go.work
go.work.sum

# IDE
.idea
.idea/
.vscode
.vscode/
*.swp
*.swo

# Local personal Claude Code settings (machine-specific overrides)
.claude/settings.local.json

# Environment
.env
.env.*

# Claude Code sandbox artifacts (root-anchored: these must only match
# loose files the sandbox drops at the repo root, not real source paths
# like internal/config/ or a top-level hooks/ directory)
/.bash_profile
/.bashrc
/.gitconfig
/.gitmodules
/.profile
/.ripgreprc
/.zprofile
/.zshrc
/HEAD
/config
/hooks
/objects
/refs
